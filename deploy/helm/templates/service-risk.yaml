apiVersion: v1
kind: Service
metadata:
  name: investment-advisor-agent-risk
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: investment-advisor-agent-risk
  ports:
    - name: http
      protocol: TCP
      port: 7001
      targetPort: 7001
  type: ClusterIP
