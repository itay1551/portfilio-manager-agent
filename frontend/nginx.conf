worker_processes auto;
error_log /var/log/nginx/error.log notice;
pid /run/nginx.pid;

events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    sendfile on;
    keepalive_timeout 65;

    server {
        listen 8080;
        server_name _;
        root /usr/share/nginx/html;
        index index.html;

        # Browser calls same-origin /api/*; nginx forwards to orchestrator on the compose network.
        # resolver + variable proxy_pass: re-resolve orchestrator after podman recreate (avoids stale IP 502).
        location /api/ {
            resolver 10.89.2.1 valid=10s ipv6=off;
            set $orchestrator_upstream http://orchestrator:5000;
            rewrite ^/api/(.*)$ /$1 break;
            proxy_pass $orchestrator_upstream;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_read_timeout 300s;
        }

        location / {
            try_files $uri $uri/ /index.html;
        }

        gzip on;
        gzip_types text/plain text/css application/json application/javascript text/xml application/xml;
    }
}
